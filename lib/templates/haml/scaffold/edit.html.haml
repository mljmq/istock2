%h3= t('edit_<%= singular_table_name %>')
%h5
  = link_to t('show'), @<%= singular_table_name %>
  \|
  = link_to t('back'), <%= index_helper %>_path

= render 'form'

%h5
  = link_to t('show'), @<%= singular_table_name %>
  \|
  = link_to t('back'), <%= index_helper %>_path